#!/usr/bin/env bash
set -e

current_branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$current_branch" = "main" ]; then
  echo "❌ Push blocked: you are on branch 'main'."
  echo "Create a milestone branch and open a PR."
  exit 1
fi

exit 0
