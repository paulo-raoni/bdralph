# E2E-04 — Refactor TaskController to separate responsibilities

## Task for the loop

Refactor `fixtures/playground/src/controllers/TaskController.ts` to separate HTTP handling from business logic.

Requirements:
- HTTP handling stays in TaskController
- Business logic moves to TaskService (or a new service layer)
- No behavior change — all existing tests pass
- New tests verify the refactored structure

## Initial fixture state

`fixtures/e2e/e2e-04/` — copy of playground with TaskController mixing HTTP and business logic.

## Success criteria

- [ ] Build passes: `npm run build`
- [ ] All tests pass
- [ ] No regression
- [ ] PR created

## Gate

`BDRALPH_E2E_MODE=no-llm` — fixture created, loop runs, refactor done, gates pass.
