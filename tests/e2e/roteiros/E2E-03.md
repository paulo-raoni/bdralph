# E2E-03 — Fix planted bug in deleteTask

## Task for the loop

Fix the bug in `fixtures/playground/src/repositories/TaskRepository.ts` in the `deleteTask` method.

The method currently deletes the wrong task when the list has more than one item. Find the bug and fix it.

## Initial fixture state

`fixtures/e2e/e2e-03/` — copy of playground with the bug already planted in `deleteTask`.

Bug: `deleteTask` uses `findIndex` incorrectly, off-by-one when the task is not the first item.

## Success criteria

- [ ] `deleteTask` correctly removes the target task regardless of its position
- [ ] Existing tests pass
- [ ] New regression test added that would have caught the bug
- [ ] PR created

## Gate

`BDRALPH_E2E_MODE=no-llm` — fixture created, loop runs, bug fixed, gates pass.
