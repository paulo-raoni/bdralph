# E2E-05 — Add priority field to Task and migrate tests

## Task for the loop

Add a `priority` field (number, 1–5) to the `Task` type in `fixtures/playground/src/types/Task.ts` and propagate the change through the entire codebase.

Requirements:
- `priority` is optional, defaults to 3
- All existing tests updated to include or handle `priority`
- New tests verify priority filtering: list tasks by priority
- No regression

## Initial fixture state

`fixtures/e2e/e2e-05/` — clean copy of playground without the `priority` field.

## Success criteria

- [ ] `Task` type has `priority` field
- [ ] All existing tests pass with the new field
- [ ] New tests cover priority filtering
- [ ] Build, lint, typecheck pass
- [ ] PR created

## Gate

`BDRALPH_E2E_MODE=no-llm` — fixture created, loop runs, migration complete, gates pass.
