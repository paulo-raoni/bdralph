# E2E-01 — Add input validation to TaskService

## Task for the loop

Add input validation to the `TaskService.createTask` method in `fixtures/playground/src/services/TaskService.ts`.

Requirements:
- Reject empty `title` with a descriptive error
- Reject `title` longer than 200 characters
- Reject negative `priority` values
- All existing tests must continue to pass
- New tests must cover the validation cases

## Initial fixture state

`fixtures/e2e/e2e-01/` — clean copy of playground with no validation in TaskService.

## Success criteria

- [ ] CI gates pass: `npm test && npm run lint && npm run typecheck`
- [ ] New tests cover the 3 validation cases
- [ ] PR created on a branch named `e2e-01/add-validation` or similar
- [ ] No regression in existing tests

## Gate

`BDRALPH_E2E_MODE=no-llm` — fixture created, loop runs, PR opened, gates pass.
`BDRALPH_E2E_MODE=with-llm` — same as above, with real Claude Code worker.
