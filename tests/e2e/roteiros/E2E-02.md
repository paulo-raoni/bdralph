# E2E-02 — Write unit tests for TaskRepository

## Task for the loop

Write unit tests for `fixtures/playground/src/repositories/TaskRepository.ts`.

Requirements:
- Coverage > 80% for TaskRepository
- Tests use Vitest
- Tests are isolated — no shared state between test cases
- Tests cover: create, read, update, delete, list, not-found cases

## Initial fixture state

`fixtures/e2e/e2e-02/` — clean copy of playground with TaskRepository having zero test coverage.

## Success criteria

- [ ] Coverage > 80% for TaskRepository
- [ ] All tests pass
- [ ] No regression in existing tests
- [ ] PR created

## Gate

`BDRALPH_E2E_MODE=no-llm` — fixture created, loop runs, gates pass.
